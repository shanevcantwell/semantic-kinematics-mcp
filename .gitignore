# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.env
.venv/
venv/

# Distribution / packaging
build/
dist/
*.egg-info/
*.egg

# Testing / coverage
.pytest_cache/
.coverage
htmlcov/
.tox/
.mypy_cache/

# IDE
.vscode/
.idea/
*.swp
*.swo

# Embedding cache
.embedding_cache/

# Experiment run outputs (verdict artifacts are reproducible from registration + null)
output/

# Copyrighted novel PDFs — must not enter the repo
data/absurdism/*.pdf

# Local embedding-run scratch (checkpoints/logs); large artifacts live off-repo (e.g. /mnt/storage)
.runs/

# Claude Code harness config — managed centrally (canonical lives outside this repo; deployed by symlink/plugin-dir), never committed per-repo
.claude/
